Streaming proxy removed

The project previously included a local streaming proxy. It was removed per request. The frontend now posts directly to the n8n webhook. If you need a proxy again, re-add a server and update the client endpoint.
